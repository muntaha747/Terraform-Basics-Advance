###############################################################################################################################################################################################
# Local variables
###############################################################################################################################################################################################

locals {
  env         = var.environment
  bucket_name = "${var.buck-name}${var.environment}"
  cidr        = var.vpc_cidr
  vpc-name    = "$(var.environment)-vpc"
}
